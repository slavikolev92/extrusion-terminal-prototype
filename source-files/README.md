# Source Files

Place the local shift-manager Excel workbook here when working on import/export behavior.

The workbook is intentionally not committed to git. The expected local filename from inspection is:

- `shift-manager-main-file.xlsm`

The tracked `excel-macros/` subfolder contains the read-only CSV export macro used with that workbook.
