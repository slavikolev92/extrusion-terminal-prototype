# Shift Manager Workbook Tasks

1. Prepare the shift-manager files in `interim-costing-process`.

2. Apply the two Excel macro tools from `interim-costing-process/excel-tools`:
   - recipe-builder macro
   - export-validation macro

3. Update columns `G`, `H`, `I`, and `J`:
   - `G`: gross canonical weight ordered by the customer, required
   - `H`: gross units ordered by the customer, optional
   - `I`: gross meters ordered, optional
   - `J`: confirm final meaning

4. Archive old data columns:
   - `P`
   - `R`
   - `S`
   - `T`
   - `U`

5. Archive process:
   - first change the file structure
   - then copy the file
   - then delete old-only historical columns from the newest file

6. Replace Elena's slower worksheet macros with the faster macros from Marco's worksheet.

7. Remove unnecessary leftover worksheets from the files.

8. Update recipe worksheets in both files:
   - one worksheet for inks
   - one worksheet for extrusion raw materials

9. Update the sales price column so it is always used and filled for every order.
